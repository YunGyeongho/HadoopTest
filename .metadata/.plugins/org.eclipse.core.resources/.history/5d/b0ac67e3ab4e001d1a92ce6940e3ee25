# -*- coding:utf-8 -*-
from flask.json import jsonify
from flask.helpers import make_response

class responseSet():
    
    def getJSON(self, result):
        
        return resBody = make_response(j = jsonify({"result" : result}))